
#include <stdio.h>
#include "arrays.h"

int mostrarArray(int array[], int cantidad)
{
    int indice;
    for(indice = 0; indice < cantidad; indice++)
    {
        printf("%d",array[cantidad]);
    }

    return 0;
}
